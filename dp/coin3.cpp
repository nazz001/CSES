#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n,x;
    int MOD=1e9+7;
    cin>>n>>x;
    vector<int>arr(n);
    vector<int>dp(x+1,0);
    map<int,int>umap;
    for(int i=0;i<arr.size();i++)
    {
        umap[arr[i]]=1;
    }
    for(int i=0;i)
    
   
    return 0;

}